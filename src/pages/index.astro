---
import CRTLayout from '../layouts/CRTLayout.astro';
// import BootSequence from '../components/BootSequence.astro';
import AppearanceControls from '../components/AppearanceControls.astro';
import NavPrompt from '../components/NavPrompt.astro';
import { GithubLogo, LinkedinLogo } from '@phosphor-icons/react';

const menuItems = [
  { key: '1', label: 'about.me', description: 'who is this guy', href: '/about' },
  { key: '2', label: 'projects/', description: "things i've built", href: '/projects' },
  { key: '3', label: 'blog/', description: 'thoughts & writeups', href: '/blog' },
  { key: '4', label: 'now.md', description: "what i'm doing now", href: '/now' },
];

const externalLinks = [
  { key: 'gh', label: 'github ↗', icon: GithubLogo, href: 'https://github.com/niroahman' },
  { key: 'li', label: 'linkedin ↗', icon: LinkedinLogo, href: 'https://linkedin.com/in/niroahman' },
];
---

<CRTLayout title="niro.codes">
  <div class="terminal">
    <header class="terminal-header">
      <span class="header-label">Choose your theme <span class="theme-arrow">--&gt;</span></span>
      <AppearanceControls />
    </header>

    <div class="terminal-body">
      {/* <BootSequence /> */}

      <div class="content-center">
        <nav id="home-menu" aria-label="Main navigation">
          <div class="logo-block">
            <pre
              class="logo"
              aria-label="NIRO ÅHMAN">
                                    ██╗
                                    ╚═╝
███╗   ██╗██╗██████╗  ██████╗      █████╗ ██╗  ██╗███╗   ███╗ █████╗ ███╗   ██╗
████╗  ██║██║██╔══██╗██╔═══██╗    ██╔══██╗██║  ██║████╗ ████║██╔══██╗████╗  ██║
██╔██╗ ██║██║██████╔╝██║   ██║    ███████║███████║██╔████╔██║███████║██╔██╗ ██║
██║╚██╗██║██║██╔══██╗██║   ██║    ██╔══██║██╔══██║██║╚██╔╝██║██╔══██║██║╚██╗██║
██║ ╚████║██║██║  ██║╚██████╔╝    ██║  ██║██║  ██║██║ ╚═╝ ██║██║  ██║██║ ╚████║
╚═╝  ╚═══╝╚═╝╚═╝  ╚═╝ ╚═════╝     ╚═╝  ╚═╝╚═╝  ╚═╝╚═╝     ╚═╝╚═╝  ╚═╝╚═╝  ╚═══╝</pre>
            <p class="subtitle">
              senior deep-stack developer with &lt;3<span class="subtitle-location"
                >&nbsp;·&nbsp; Akaa, Finland</span
              >
            </p>
          </div>

          <ul class="menu-list">
            {
              menuItems.map((item) => (
                <li>
                  <a href={item.href} class="menu-item" data-key={item.key}>
                    <span class="menu-key">[{item.key}]</span>
                    <span class="menu-label">{item.label}</span>
                    <span class="menu-desc">{item.description}</span>
                  </a>
                </li>
              ))
            }
          </ul>

          <div class="ext-links" role="group" aria-label="External links">
            {
              externalLinks.map((link) => (
                <a
                  href={link.href}
                  class="ext-link"
                  data-key={link.key}
                  target="_blank"
                  rel="noopener noreferrer"
                >
                  <link.icon
                    size={18}
                    weight="duotone"
                    style={{ verticalAlign: 'middle', marginRight: '6px' }}
                  />
                  {link.label}
                </a>
              ))
            }
          </div>

          <a href="/niro-exe" class="game-entry" data-key="play">
            <span class="menu-key">[▶]</span>
            <span class="menu-label">NIRO.EXE</span>
            <span class="menu-desc game-coin">[ INSERT COIN ]</span>
          </a>
        </nav>
      </div>

      <NavPrompt />
    </div>
  </div>
</CRTLayout>

<style>
  .terminal {
    display: flex;
    flex-direction: column;
    height: 100%;
    max-width: 640px;
    margin: 0 auto;
    padding: 2rem 1.5rem;
    box-sizing: border-box;
  }

  .terminal-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 2rem;
    font-size: 1rem;
  }

  .header-label {
    font-size: 0.875rem;
    color: var(--phosphor);
    opacity: 0.7;
  }

  :global([data-theme='light']) .header-label {
    color: color-mix(in oklch, var(--phosphor) 80%, black);
  }

  .terminal-body {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .content-center {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 2rem;
    padding-top: 4rem;
  }

  #home-menu {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .menu-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .menu-item,
  .game-entry {
    display: grid;
    grid-template-columns: 3rem 10rem 1fr;
    gap: 0.5rem;
    align-items: baseline;
    color: var(--phosphor);
    text-decoration: none;
    font-size: 1rem;
    padding: 0.2rem 0;
    transition: opacity 0.15s;
  }

  .menu-item:hover,
  .game-entry:hover {
    opacity: 0.75;
  }

  .menu-item[data-selected],
  .game-entry[data-selected] {
    opacity: 1;
    background: color-mix(in srgb, var(--phosphor) 10%, transparent);
    border-radius: 2px;
  }

  .menu-key {
    opacity: 0.5;
  }

  .menu-label {
    font-weight: bold;
  }

  .menu-desc {
    color: var(--phosphor-dim);
    font-size: 0.9rem;
  }

  .ext-links {
    display: flex;
    gap: 2rem;
    border: 1px dashed var(--dim);
    padding: 0.6rem 1rem;
    font-size: 0.875rem;
  }

  .ext-link {
    display: inline-flex;
    align-items: center;
    color: var(--phosphor-dim);
    text-decoration: none;
    transition: color 0.15s;
  }

  .ext-link:hover {
    color: var(--phosphor);
  }

  .ext-link[data-selected] {
    color: var(--phosphor);
    background: color-mix(in srgb, var(--phosphor) 10%, transparent);
    border-radius: 2px;
  }

  .game-coin {
    color: var(--amber);
    animation: blink 1.2s step-end infinite;
  }

  .logo-block {
    margin-bottom: 0.5rem;
  }

  .logo {
    font-size: 8px;
    line-height: 1.2;
    white-space: pre;
    color: var(--phosphor);
    text-shadow:
      0 0 8px var(--phosphor),
      0 0 20px color-mix(in srgb, var(--phosphor) 40%, transparent);
    margin: 0 0 1.5rem;
    animation: flicker 9s infinite;
  }

  @media (max-width: 600px) {
    .terminal {
      overflow-y: auto;
    }

    .logo {
      font-size: 5.5px;
      max-width: 100%;
      overflow-x: auto;
    }

    .content-center {
      padding-top: 1.5rem;
    }

    .menu-item,
    .game-entry {
      grid-template-columns: 2.5rem 7rem 1fr;
    }

    .subtitle {
      white-space: normal;
      overflow: visible;
      text-overflow: clip;
    }

    .terminal-header {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.4rem;
    }

    .terminal-header :global(.appearance-controls) {
      align-self: flex-end;
    }

    .theme-arrow {
      font-size: 0;
    }

    .theme-arrow::before {
      content: '↓';
      font-size: 0.875rem;
    }
  }

  .subtitle {
    font-size: 0.8rem;
    color: var(--phosphor-dim);
    border-bottom: 1px solid var(--dim);
    padding-bottom: 0.5rem;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .subtitle-location {
    white-space: nowrap;
  }
</style>
